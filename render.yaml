databases:
  - name: breathe-esg-db
    plan: free
    region: ohio

services:
  # ==========================================
  # Django Backend API
  # ==========================================
  - type: web
    name: breathe-esg-backend
    env: python
    plan: free
    region: ohio
    buildCommand: "./build.sh"
    startCommand: "python manage.py migrate && python manage.py seed_data && gunicorn breatheESG.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: breathe-esg-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "breathe-esg-backend-t6jr.onrender.com,localhost,127.0.0.1"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "False"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://breathe-esg-frontend-g3wu.onrender.com,http://localhost:5173,http://127.0.0.1:5173"
      - key: PYTHON_VERSION
        value: "3.11.9"

  # ==========================================
  # React Frontend Dashboard
  # ==========================================
  - type: static
    name: breathe-esg-frontend
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: "./frontend/dist"
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://breathe-esg-backend-t6jr.onrender.com/api"
      - key: NODE_VERSION
        value: "20"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
